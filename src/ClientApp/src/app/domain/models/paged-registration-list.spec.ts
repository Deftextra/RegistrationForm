import { PagedRegistrationList } from './paged-registration-list';

describe('PagedRegistrationList', () => {
  it('should create an instance', () => {
    expect(new PagedRegistrationList()).toBeTruthy();
  });
});
